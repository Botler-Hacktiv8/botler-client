import { USER_REGISTER, USER_LOGIN, USER_LOGOUT, GET_PROFILE } from './../action-type';
import axios from 'axios';

const baseUrl = `http://ec2-18-191-188-60.us-east-2.compute.amazonaws.com`;

const userRegisterAction = () => {
  return dispatch => {
    
  }
}

